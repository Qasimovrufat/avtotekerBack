@using res = Wheel.User.Properties.Resources
@{
    int i = 0;
}
<section id="slider">
    <div class="owl-carousel owl-theme slider-active">
        <div class="single-slider">
            <img src="/Public/img/avtoteker-slider.png" alt="Alternate Text" />
        </div>
        <div class="single-slider">
            <img src="/Public/img/5cc89651bdcd2.png" alt="Alternate Text" />
        </div>
    </div>
</section>

<section id="search">
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <div class="search-area text-center">
                    <div class="title">
                        <h3>
                            @res.search
                        </h3>
                    </div>
                    <div class="tab-list">
                        <a class="active" href="#searchTyres">@res.searchTyre</a>
                        <a href="#searchCar">@res.searchCar (@res.soon) </a>
                    </div>
                    <div class="tab-content active" id="searchTyres">
                        <div class="tab-search">
                            <form action="/tyres/" method="GET" id="searchbytyres">
                                <div class="row">
                                    <div class="col-lg-3">
                                        <div class="form-group">
                                            <div class="name">
                                                <img src="~/Public/img/section-width.svg" />
                                                <p>@res.width</p>
                                            </div>
                                            <select data-live-search="true" name="Width" id="WidthId" class="selectpicker" data-width="100%"></select>
                                        </div>
                                    </div>
                                    <div class="col-lg-3">
                                        <div class="form-group">
                                            <div class="name">
                                                <img src="~/Public/img/aspect-ratio.svg" />
                                                <p>@res.height</p>
                                            </div>
                                            <select data-live-search="true" name="Height" id="HeightId" data-width="100%" class="selectpicker"></select>
                                        </div>
                                    </div>
                                    <div class="col-lg-3">
                                        <div class="form-group">
                                            <div class="name">
                                                <img src="~/Public/img/diameter.svg" />
                                                <p>@res.radius</p>
                                            </div>
                                            <select data-live-search="true" name="Radius" id="RadiusId" data-width="100%" class="selectpicker"></select>
                                        </div>
                                    </div>
                                    <div class="col-lg-3">
                                        <div class="form-group">
                                            <div class="name">
                                                <img src="~/Public/img/brands.svg" />
                                                <p>@res.brand</p>
                                            </div>
                                            <select data-live-search="true" name="BrandId" id="BrandId" data-width="100%" class="selectpicker">
                                                <option value="" selected disabled hidden>@res.model</option>
                                            </select>
                                        </div>
                                    </div>
                                </div>


                                <button type="submit" id="tyreSearchButton" class="btnSearch">@res.search </button>
                                <a href="" class="Reset">@res.clearAll</a>

                            </form>
                        </div>

                    </div>
                    <div class="tab-content" id="searchCar">
                        <div class="tab-search" id="car">
                            <form id="searchbycar" action="/tyres/" method="GET">
                                <div class="row">
                                    <div class="col-lg-3">
                                        <div class="form-group">
                                            <div class="name">
                                                <p>@res.brand</p>
                                            </div>
                                            <select name="BrandIdCar" id="BrandIdCar" data-live-search="true" class="selectpicker">
                                                <option selected disabled value="0"> @res.car</option>
                                                @foreach (var car in Model.carbrand)
                                                {
                                                    <option value="@car.Id">@car.Name</option>
                                                }
                                            </select>
                                        </div>
                                    </div>
                                    <div class="col-lg-3">
                                        <div class="form-group">
                                            <div class="name">

                                                <p>@res.model</p>
                                            </div>
                                            <select name="ModelIdCar" disabled id="ModelIdCar" data-live-search="true" class="selectpicker"></select>
                                        </div>
                                    </div>
                                    <div class="col-lg-3">
                                        <div class="form-group">
                                            <div class="name">
                                                <p>@res.year</p>
                                            </div>
                                            <select name="YearCar" disabled id="YearCar" data-live-search="true" class="selectpicker"></select>
                                        </div>
                                    </div>
                                    <div class="col-lg-3">
                                        <div class="form-group">
                                            <div class="name">
                                                <p>@res.engine</p>
                                            </div>
                                            <select name="BanCar" disabled id="BanCar" data-live-search="true" class="selectpicker"></select>
                                        </div>
                                    </div>
                                </div>

                            </form>
                        </div>
                    </div>
                </div>

            </div>
        </div>
    </div>
</section>

@*<section id="search">
    <div class="overlay"></div>
    <div class="container">
        <div class="row">
            <div class="col-lg-4">
                <div class="search-area text-center">
                    <div class="tab-list">
                        <a class="active" href="#searchTyres"> @res.search  @res.tyres</a>
                        <a href="#searchCar"> @res.search   @res.car </a>
                    </div>
                    <div class="tab-content active" id="searchTyres">
                        <div class="tab-search">
                            <form action="/tyres/" method="GET" id="searchbytyres">
                                <div class="form-group">
                                    <select data-live-search="true" name="Width" id="WidthId" class="selectpicker" data-width="100%"></select>
                                </div>
                                <div class="form-group">
                                    <select data-live-search="true" name="Height" id="HeightId" class="selectpicker" data-width="100%"></select>
                                </div>
                                <div class="form-group">
                                    <select data-live-search="true" name="Radius" id="RadiusId" class="selectpicker" data-width="100%"></select>
                                </div>
                                <div class="form-group">
                                    <select data-live-search="true" name="BrandId" id="BrandId" class="selectpicker" data-width="100%">
                                        <option value="" selected disabled hidden>@res.brand</option>
                                    </select>
                                </div>
                                <div class="form-group">
                                    <select data-live-search="true" name="ModelId" id="ModelId" class="selectpicker" data-width="100%">
                                        <option value="" selected disabled hidden>@res.model</option>

                                    </select>
                                </div>

                                <div class="form-group range-slider">
                                    <p>@res.price</p>
                                    <b>10</b>
                                    <input data-slider-tooltip="show" name="priceArray" id="ex2" type="text" class="span2" value="" data-slider-min="10" data-slider-max="1000"
                                           data-slider-step="5" data-slider-value="[0,1000]" />
                                    <b>1000</b>
                                </div>
                                <hr>
                                <button id="tyreSearchButton" type="submit" class="btnSearch">    @res.search </button>
                                <a href="" class="Reset">Reset all filters</a>
                            </form>
                        </div>
                    </div>
                    <div class="tab-content" id="searchCar">
                        <div class="tab-search" id="car">
                            <form id="searchbycar" action="/tyres/" method="GET">
                                <div class="form-group">
                                    <select data-live-search="true" class="selectpicker" name="BrandIdCar" id="BrandIdCar">
                                        <option selected disabled value="0">res.pleaseSelect @res.car</option>

                                        @foreach (var car in Model.carbrand)
                                        {
                                            <option value="@car.Id">@car.Name</option>
                                        }
                                    </select>
                                </div>
                                <div class="form-group">
                                    <select data-live-search="true" class="selectpicker" name="ModelIdCar" disabled id="ModelIdCar"></select>
                                </div>
                                <div class="form-group">
                                    <select data-live-search="true" class="selectpicker" name="YearCar" disabled id="YearCar"></select>
                                </div>
                                <div class="form-group">
                                    <select data-live-search="true" class="selectpicker" name="BanCar" disabled id="BanCar"></select>
                                </div>


                                <hr>

                            </form>
                        </div>
                    </div>
                </div>
            </div>
            <div class="col-lg-7 align-self-center offset-lg-1">
                <div class="title">
                    <h1>@res.customWheelsRims</h1>
                </div>
            </div>

        </div>
    </div>
</section>*@
@*<section id="season">
    <div class="container">
        <div class="row">
            <div class="col-lg-4">
                <div class="season-box">
                    <a href="">
                        <div class="upper">
                            <div class="img">
                                <img src="~/Public/img/summer.svg" alt="">
                            </div>
                        </div>
                        <div class="lower">
                            <div class="title">
                                <h3>@res.summer @res.tyresHome</h3>
                            </div>
                        </div>
                    </a>
                </div>
            </div>
            <div class="col-lg-4">
                <div class="season-box">
                    <a href="">
                        <div class="upper">
                            <div class="img">
                                <img src="~/Public/img/winter.svg" alt="">
                            </div>
                        </div>
                        <div class="lower">
                            <div class="title">
                                <h3>@res.winter @res.tyresHome</h3>
                            </div>
                        </div>
                    </a>

                </div>
            </div>
            <div class="col-lg-4">
                <div class="season-box">
                    <a href="">
                        <div class="upper">
                            <div class="img">
                                <img src="~/Public/img/custom.svg" alt="">
                            </div>
                        </div>
                        <div class="lower">
                            <div class="title">
                                <h3>@res.customTyres</h3>
                            </div>
                        </div>
                    </a>
                </div>
            </div>
        </div>
    </div>
</section>*@
<section id="brand-carousel">
    <div class="container">
        <div class="brand-logo-active owl-carousel">
            <div class="single-brand-logo">
                <a href="/tyres/?BrandId=138">
                    <img src="~/Public/img/Bridgestone-logo-5500x1500.png" alt="">
                </a>
            </div>
            <div class="single-brand-logo">
                <a href="/tyres/?BrandId=141">
                    <img src="~/Public/img/continental-png-continental-logo-8333.png" alt="">
                </a>
            </div>
            <div class="single-brand-logo">
                <a href="/tyres/?BrandId=145">
                    <img src="~/Public/img/dunlop_logo_png_426199.png" alt="">
                </a>
            </div>
            <div class="single-brand-logo">
                <a href="/tyres/?BrandId=184">
                    <img src="~/Public/img/michelin-png-michelin-png-2000.png" alt="">
                </a>
            </div>
            <div class="single-brand-logo">
                <a href="/tyres/?BrandId=190">
                    <img src="~/Public/img/Pirelli-logo-2560x1440.png" alt="">
                </a>
            </div>
            <div class="single-brand-logo">
                <a href="/tyres/?BrandId=216">
                    <img src="~/Public/img/Yokohama-logo-5100x800.png" alt="">
                </a>
            </div>

        </div>
    </div>
</section>
@*<section id="find-all">
    <div class="overlay"></div>
    <div class="container">
        <div class="row text-center align-items-center ">
            <div class="col-lg-12">
                <h2>@res.findWheelTyre</h2>
                <h4>@res.forAll</h4>
                <a href="">@res.seeAll </a>
            </div>
        </div>
    </div>
</section>*@
@*<section id="products">
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <div class="title">
                    @res.featuredProducts
                </div>
            </div>

            @foreach (var tyre in Model.tyres.tyreList)
            {
                i++;
                if (i == 9)
                {
                    break;
                }
                <div class="col-lg-3">
                    <a href="/tyres/details/?id=@tyre.Id">
                        <div class="product-box">
                            <div class="upper">
                                <div class="img">
                                    <img src="/adminpr/@(tyre.Images.Count>0 ? tyre.Images[0].Path : "360_forged_-_mesh_8_custom_painted_close-up_1_large68a8.png")" alt="@tyre.ModelName">
                                </div>
                            </div>
                            <div class="lower">
                                <div class="brand">
                                    <h6>
                                        @tyre.BrandName
                                    </h6>
                                </div>
                                <div class="size">
                                    <p>@tyre.ModelName @tyre.Width<span>/</span>@tyre.Height<span>R</span>@tyre.Radius</p>

                                </div>
                                <div class="price">
                                    <span> @(((tyre.Price - (tyre.Price / 100 * tyre.Sale)) * Model.currency).ToString("0")) AZN</span>

                                    @if (tyre.Price > (tyre.Price - (tyre.Price / 100 * tyre.Sale)))
                                    {
                                        <span class="line-through">@((tyre.Price * Model.currency).ToString("0")) AZN</span>

                                    }


                                </div>
                                <a class="details" href="/tyres/details/?id=@tyre.Id">@res.details</a>
                            </div>
                        </div>
                    </a>
                </div>

            }





        </div>
    </div>
</section>*@
<section id="services">
    <div class="overlay"></div>
    <div class="container">
        <div class="row text-center">
            <div class="col-lg-12">
                <div class="title">
                    <h1>
                        @res.serviceHeading
                    </h1>
                </div>
            </div>
            <div class="col-lg-6">
                <div class="title">
                    <h6>@res.servicesTitle3</h6>

                </div>
                <div class="content">
                    <p>
                        @res.servicesText3
                    </p>
                </div>
            </div>
            <div class="col-lg-6">
                <div class="title">
                    <h6>
                        @res.servicesTitle2
                    </h6>

                </div>
                <div class="content">
                    <p>
                        @res.servicesText1
                    </p>
                </div>
            </div>
         
        </div>
    </div>
</section>
@*<section id="terms">
    <div class="container">
        <div class="row">
            <div class="col-lg-4 text-center">
                <div class="box">
                    <div class="icon">
                        <i class="fas fa-phone"></i>
                    </div>
                    <div class="title">
                        <p> (077) 592 7444</p>

                    </div>
                    <div class="content">
                        <p> @res.roundClock</p>
                    </div>
                </div>
            </div>
            <div class="col-lg-4 text-center">
                <div class="box">
                    <div class="icon">
                        <i class="fas fa-undo-alt"></i>
                    </div>
                    <div class="title">
                        <p>
                            @res.returnExchange
                        </p>

                    </div>
                    <div class="content">
                        <p>@res.sevenDay @res.easyReturn</p>
                    </div>
                </div>
            </div>
            <div class="col-lg-4 text-center">
                <div class="box">
                    <div class="icon">
                        <i class="far fa-thumbs-up"></i>
                    </div>
                    <div class="title">
                        <p>
                            @res.satisfaction
                        </p>

                    </div>
                    <div class="content">
                        <p> @res.guaranteed</p>
                    </div>
                </div>
            </div>

        </div>
    </div>
</section>*@
<section id="map">
    <div class="col-lg-12">
        <div id="google_map"></div>
    </div>
</section>
<div id="newCarModal" class="modal fade" role="dialog">
    <div class="modal-dialog">
        <!-- Modal content-->
        <div class="modal-content">
            <div class="modal-header">
                <h4 class=" modal-title">@*@res.pleaseSelect*@ @res.tyres</h4>
                <button type="button" class="close" data-dismiss="modal">&times;</button>

            </div>
            <div class="modal-body">
                <div class="card">
                    <div class="card-header frontPanel">Front</div>
                    <div class="card-body">
                        <ul id="frontPanel"></ul>
                    </div>
                </div>
                <div class="card">
                    <div class="card-header">Rear</div>
                    <div class="card-body rearPanel">
                        <ul id="rearPanel"></ul>
                    </div>
                </div>
            </div>
            <div class="clearfix"></div>
        </div>
    </div>
</div>
@Html.Partial("_Footer")

@section script{
    <script>
            $.fn.serializeObject = function () {
                var o = {};
                var a = this.serializeArray();
                $.each(a, function () {
                    if (o[this.name]) {
                        if (!o[this.name].push) {
                            o[this.name] = [o[this.name]];
                        }
                        o[this.name].push(this.value || '');
                    } else {
                        o[this.name] = this.value || '';
                    }
                });
                return o;
            };
            // Instantiate a slider
            var mySlider = $("#price").slider({
                // tooltip: 'always',
                tooltip_position: 'bottom',

            }).on("slideStop", function (slideEvt) { refreshList(); });


            $("#searchbytyres select").change(function () {
                refreshList($(this));
            });

            $("#tyreSearchButton").click(function () {
                $("#searchbytyres").submit();
            })
            function refreshList(e) {

                var formData = $("#searchbytyres").serializeObject();
           
                $.ajax({
                    url: "/json/searchbytyres",
                    type: "GET",
                    data: formData,
                    cache: false,
                    processData: false,
                    contentType:false,
                    success: function (response) {
                        var oldValue = $(e).html();
                        $(".selectpicker.clear").html("");
                        if (!e) {
                            for (var i = 0; i < response.brand.length; i++) {
                                $("#BrandId").append('<option value="' + response.brand[i].Id + '">' + response.brand[i].Name + '</option>');
                            }
                        }
                        for (var i = 0; i < response.model.length; i++) {
                            $("#ModelId").append('<option value="' + response.model[i].Id + '">' + response.model[i].Name + '</option>');
                        }
                        for (var i = 0; i < response.width.length; i++) {
                            $("#WidthId").append('<option value="' + response.width[i].Id + '">' + response.width[i].Name + '</option>');
                        }
                        for (var i = 0; i < response.height.length; i++) {
                            $("#HeightId").append('<option value="' + response.height[i].Id + '">' + response.height[i].Name + '</option>');
                        }
                        for (var i = 0; i < response.radius.length; i++) {
                            $("#RadiusId").append('<option value="' + response.radius[i].Id + '">' + response.radius[i].Name + '</option>');
                        }

                        //for (var i = 0; i < response.usage.length; i++) {
                        //    $("#UsageId").append('<option value="' + response.usage[i].Id + '">' + response.usage[i].Name + '</option>');
                        //}

                        $(e).html(oldValue);

                        $('#BrandId').val(formData.BrandId);
                        $('#ModelId').val(formData.ModelId);
                        $('#WidthId').val(formData.Width);
                        $('#HeightId').val(formData.Height);
                        $('#RadiusId').val(formData.Radius);
                        $('#UsageId').val(formData.Usage);
                        //todo set selecetd item
                        $(".selectpicker").selectpicker("refresh");
                    }
                })
            }
        $(document).ready(function () {
                refreshList();
                initialize();
            })

            $("#BrandIdCar").change(function () {
                $("#ModelIdCar").html('<option selected disabled value="0"> @res.model</option>');
                $("#YearCar").html('<option selected disabled value="0"> @res.winter</option>');
                $("#BanCar").html('<option selected disabled value="0"> @res.car</option>');
                var id = $(this).val();
                // if (id != 0) {
                $.ajax({
                    url: "/Json/listModels",
                    data: "id=" + id,
                    type: "GET",
                    success: function (result) {
                        $.each(result.model, function (index, model) {
                            $("#ModelIdCar").append('<option value="' + model.Id + '">' + model.Name + '</option>');
                        });
                        $("#ModelIdCar").removeAttr("disabled");
                        $("#ModelIdCar").selectpicker("refresh");

                    }
                })
                //  }
                //  else
                //  {
                //      $("#ModelIdCar").addAttr("disabled");
                //  }
            });

            $("#ModelIdCar").change(function () {
                $("#YearCar").html('<option selected disabled value="0">@res.winter</option>');
                $("#BanCar").html('<option selected disabled value="0"> @res.car</option>');
                var id = $(this).val();
                $.ajax({
                    url: "/Json/listYears",
                    data: "id=" + id,
                    type: "GET",
                    success: function (result) {
                        $.each(result.year, function (index, year) {
                            $("#YearCar").append('<option value="' + year.Id + '">' + year.Year + '</option>');
                        });
                        $("#YearCar").removeAttr("disabled");
                        $("#YearCar").selectpicker("refresh");

                    }
                })
            });

            $("#YearCar").change(function () {
                $("#BanCar").html('<option selected disabled value="0">@res.car</option>');
                var id = $(this).val();
                $.ajax({
                    url: "/Json/listBans",
                    data: "id=" + id,
                    type: "GET",
                    success: function (result) {
                        $.each(result.ban, function (index, ban) {
                            $("#BanCar").append('<option value="' + ban.Id + '">' + ban.Name + '</option>');
                        });
                        $("#BanCar").removeAttr("disabled");
                        $("#BanCar").selectpicker("refresh");

                    }
                })
            });

            $("#BanCar").change(function () {
                var id = $(this).val();
                var frontTyre = [];
                var rearTyre = [];
                $.ajax({
                    url: "/Json/listWheels",
                    data: "id=" + id,
                    type: "GET",
                    success: function (result) {
                        $.each(result.wheel, function (index, wheel) {
                            var wheels = JSON.parse(wheel.wheels);
                            $.each(wheels, function (i, tyres) {
                                $.each(tyres, function (index, tyre) {
                                    $.each(tyre, function (ind, thisTyre) {
                                        $.each(thisTyre.front, function (tindex, front) {
                                            // frontTyre.push({ width: front.tire_width[0], height: front.tire_aspect_ratio[0], radius: front.rim_diameter[0] });
                                            if (front.tire_width[0] != "") {
                                                frontTyre.push('<li><a class="btn btn-primary" href="/tyres?width=' + front.tire_width[0] + '&height=' + front.tire_aspect_ratio[0] + '&radius=' + front.rim_diameter[0] + '">' + front.tire_width[0] + '/' + front.tire_aspect_ratio[0] + 'R' + front.rim_diameter[0] + '</a></li>')
                                            }//console.log(frontTyre)
                                        })
                                        $.each(thisTyre.rear, function (tindex, rear) {
                                            //rearTyre.push({ width: rear.tire_width[0], height: rear.tire_aspect_ratio[0], radius: rear.rim_diameter[0] });
                                            if (rear.tire_width[0] != "") {
                                                rearTyre.push('<li><a  class="btn btn-primary"  href="/tyres?width=' + rear.tire_width[0] + '&height=' + rear.tire_aspect_ratio[0] + '&radius=' + rear.rim_diameter[0] + '">' + rear.tire_width[0] + '/' + rear.tire_aspect_ratio[0] + 'R' + rear.rim_diameter[0] + '</a></li>')
                                            }// console.log(rearTyre)
                                        })
                                    })
                                })

                            })
                        });
                        $("#frontPanel").html(frontTyre.join(" "));
                        $("#rearPanel").html(rearTyre.join(" "));
                        $("#newCarModal").modal({ backdrop: false });
                    }
                })
            });
    </script>

}
<script>

            //$.fn.serializeObject = function () {
            //    var o = {};
            //    var a = this.serializeArray();
            //    $.each(a, function () {
            //        if (o[this.name]) {
            //            if (!o[this.name].push) {
            //                o[this.name] = [o[this.name]];
            //            }
            //            o[this.name].push(this.value || '');
            //        } else {
            //            o[this.name] = this.value || '';
            //        }
            //    });
            //    return o;
            //};
            //// Instantiate a slider
            //var mySlider = $("#price").slider({
            //    // tooltip: 'always',
            //    tooltip_position: 'bottom',

            //}).on("slideStop", function (slideEvt) { refreshList(); });


            $("#searchbytyres select").change(function () {
                    refreshList($(this));
            });

            $("#tyreSearchButton").click(function () {
                $("#searchbytyres").submit();
            })
            function refreshList(e) {

                var formData = $("#searchbytyres").serializeObject();
                //console.log(e)

                $.ajax({
                    url: "/json/searchbytyres",
                    type: "GET",
                    data: formData,
                    cache: false,
                    processData: false,
                    contentType: false,
                    dataType: "json",
                    success: function (response) {

                        var oldValue = $(e).html();
                        $(".selectpicker.clear").html("");
                        if (!e) {
                            for (var i = 0; i < response.brand.length; i++) {
                                $("#BrandId").append('<option value="' + response.brand[i].Id + '">' + response.brand[i].Name + '</option>');
                            }
                        }
                        for (var i = 0; i < response.model.length; i++) {
                            $("#ModelId").append('<option value="' + response.model[i].Id + '">' + response.model[i].Name + '</option>');
                        }
                        for (var i = 0; i < response.width.length; i++) {
                            $("#WidthId").append('<option value="' + response.width[i].Id + '">' + response.width[i].Name + '</option>');
                        }
                        for (var i = 0; i < response.height.length; i++) {
                            $("#HeightId").append('<option value="' + response.height[i].Id + '">' + response.height[i].Name + '</option>');
                        }
                        for (var i = 0; i < response.radius.length; i++) {
                            $("#RadiusId").append('<option value="' + response.radius[i].Id + '">' + response.radius[i].Name + '</option>');
                        }

                        //for (var i = 0; i < response.usage.length; i++) {
                        //    $("#UsageId").append('<option value="' + response.usage[i].Id + '">' + response.usage[i].Name + '</option>');
                        //}

                        $(e).html(oldValue);

                        $('#BrandId').val(formData.BrandId);
                        $('#ModelId').val(formData.ModelId);
                        $('#WidthId').val(formData.Width);
                        $('#HeightId').val(formData.Height);
                        $('#RadiusId').val(formData.Radius);
                        $('#UsageId').val(formData.Usage);
                        //todo set selecetd item
                        $(".selectpicker").selectpicker("refresh");
                    }
                })
            }
        $(document).ready(function () {
                refreshList();
                initialize();
            })

            $("#BrandIdCar").change(function () {
                $("#ModelIdCar").html('<option selected disabled value="0"> @res.model</option>');
                $("#YearCar").html('<option selected disabled value="0"> @res.winter</option>');
                $("#BanCar").html('<option selected disabled value="0"> @res.car</option>');
                var id = $(this).val();
                // if (id != 0) {
                $.ajax({
                    url: "/Json/listModels",
                    data: "id=" + id,
                    type: "GET",
                    success: function (result) {
                        $.each(result.model, function (index, model) {
                            $("#ModelIdCar").append('<option value="' + model.Id + '">' + model.Name + '</option>');
                        });
                        $("#ModelIdCar").removeAttr("disabled");
                        $("#ModelIdCar").selectpicker("refresh");

                    }
                })
                //  }
                //  else
                //  {
                //      $("#ModelIdCar").addAttr("disabled");
                //  }
            });

            $("#ModelIdCar").change(function () {
                $("#YearCar").html('<option selected disabled value="0">@res.winter</option>');
                $("#BanCar").html('<option selected disabled value="0"> @res.car</option>');
                var id = $(this).val();
                $.ajax({
                    url: "/Json/listYears",
                    data: "id=" + id,
                    type: "GET",
                    success: function (result) {
                        $.each(result.year, function (index, year) {
                            $("#YearCar").append('<option value="' + year.Id + '">' + year.Year + '</option>');
                        });
                        $("#YearCar").removeAttr("disabled");
                        $("#YearCar").selectpicker("refresh");

                    }
                })
            });

            $("#YearCar").change(function () {
                $("#BanCar").html('<option selected disabled value="0">@res.car</option>');
                var id = $(this).val();
                $.ajax({
                    url: "/Json/listBans",
                    data: "id=" + id,
                    type: "GET",
                    success: function (result) {
                        $.each(result.ban, function (index, ban) {
                            $("#BanCar").append('<option value="' + ban.Id + '">' + ban.Name + '</option>');
                        });
                        $("#BanCar").removeAttr("disabled");
                        $("#BanCar").selectpicker("refresh");

                    }
                })
    });



            $("#BanCar").change(function () {
                var id = $(this).val();
                var frontTyre = [];
                var rearTyre = [];
                $.ajax({
                    url: "/Json/listWheels",
                    data: "id=" + id,
                    type: "GET",
                    success: function (result) {
                        $.each(result.wheel, function (index, wheel) {
                            var wheels = JSON.parse(wheel.wheels);
                            $.each(wheels, function (i, tyres) {
                                $.each(tyres, function (index, tyre) {
                                    $.each(tyre, function (ind, thisTyre) {
                                        $.each(thisTyre.front, function (tindex, front) {
                                            // frontTyre.push({ width: front.tire_width[0], height: front.tire_aspect_ratio[0], radius: front.rim_diameter[0] });
                                            if (front.tire_width[0] != "") {
                                                frontTyre.push('<li><a class="btn btn-primary" href="/tyres?width=' + front.tire_width[0] + '&height=' + front.tire_aspect_ratio[0] + '&radius=' + front.rim_diameter[0] + '">' + front.tire_width[0] + '/' + front.tire_aspect_ratio[0] + 'R' + front.rim_diameter[0] + '</a></li>')
                                            }//console.log(frontTyre)
                                        })
                                        $.each(thisTyre.rear, function (tindex, rear) {
                                            //rearTyre.push({ width: rear.tire_width[0], height: rear.tire_aspect_ratio[0], radius: rear.rim_diameter[0] });
                                            if (rear.tire_width[0] != "") {
                                                rearTyre.push('<li><a  class="btn btn-primary"  href="/tyres?width=' + rear.tire_width[0] + '&height=' + rear.tire_aspect_ratio[0] + '&radius=' + rear.rim_diameter[0] + '">' + rear.tire_width[0] + '/' + rear.tire_aspect_ratio[0] + 'R' + rear.rim_diameter[0] + '</a></li>')
                                            }// console.log(rearTyre)
                                        })
                                    })
                                })

                            })
                        });
                        $("#frontPanel").html(frontTyre.join(" "));
                        $("#rearPanel").html(rearTyre.join(" "));
                        $("#newCarModal").modal({ backdrop: false });
                    }
                })
            });
</script>

<script src="http://maps.googleapis.com/maps/api/js?key=AIzaSyAUUQdy5XtUuo0wzvwKmm-qjgBo5hVICVA"></script>
<script type="text/javascript">
    var map;

    function initialize() {

        var latlng = new google.maps.LatLng(40.409793, 49.866322);

        var mapOptions = {
            center: latlng,
            zoom: 17,
            mapTypeId: google.maps.MapTypeId.ROADMAP,
            panControl: false,
            zoomControl: true,
            mapTypeControl: false,
            scaleControl: false,
            scrollwheel: false,
        };

        map = new google.maps.Map(document.getElementById('google_map'), mapOptions);

        var geocoder = new google.maps.Geocoder();
        var myLatLng = { lat: 40.409793, lng: 49.866322 };
        //40.409793, 49.866322
        var address = '102 Yusif Vazir Chamanzaminli';
        var marker = new google.maps.Marker({ map: map, position: myLatLng, icon: markerImage });

        var markerImage = new google.maps.MarkerImage(
            '/Content/marker7d44.png',
            new google.maps.Size(88, 88), // marker size
            new google.maps.Point(0, 0),
            new google.maps.Point(37, 88) // marker center point
        );

        var styles = [
            {
                stylers: [
                    { saturation: -85 },
                    { lightness: 13 }
                ]
            }, {
                featureType: "road.highway",
                elementType: "geometry",
                stylers: [
                    { color: '#ffffff' }
                ]
            }
        ];

        map.setOptions({ styles: styles });
    }
</script>

